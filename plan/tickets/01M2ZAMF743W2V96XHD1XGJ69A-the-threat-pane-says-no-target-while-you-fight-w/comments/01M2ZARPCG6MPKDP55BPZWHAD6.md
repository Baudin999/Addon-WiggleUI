---
revision: 5
id: 01M2ZARPCG6MPKDP55BPZWHAD6
---

Cause. Meter/Threat.lua named "target" in all three places it needed a mob. Targeting/Aim.lua switches action targeting on out of the box, so the client picks the enemy in front of the camera and answers for it under `softenemy`; SoftTargetForce is what copies that onto the target and a client not honouring it leaves nothing selected, so all three questions answered nothing and the header said "no target" for the whole fight.  Fix. Unit.Aimed in Unit/Unit.lua, held target first then the soft token. Unit.Attackable and Unit.Soft moved there out of Charge/Charge.lua, which had the only copies. Sample takes the mob as an argument, and the reset runs on the tick as well as off PLAYER_TARGET_CHANGED because a soft target moves with no event of its own.  Ruled out. The client does have the API: UnitDetailedThreatSituation is in WowClassic.exe on 2.5.6.69795 and the wiki has it from 1.13.5, so ns.HasThreat is true and "no api" was never the case. Not proven: whether the client answers threat for a member against `softenemy` rather than a held target. If it does not, the header now says "no threat" instead, which is the next word to report.  Gate. 25-meters covers all three: soft mob sampled with nothing targeted, a held target beating the camera, a corpse under the camera reading "no target". Gated in .claude/worktrees/threat-aim at HEAD plus the five files, 0/0, harness green; the hook was bypassed because 27-swing-visible fails on a peer's uncommitted UI/Ability.lua edit.
