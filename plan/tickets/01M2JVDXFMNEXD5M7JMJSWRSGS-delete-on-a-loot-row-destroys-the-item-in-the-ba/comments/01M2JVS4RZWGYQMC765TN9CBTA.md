---
revision: 5
id: 01M2JVS4RZWGYQMC765TN9CBTA
---

Cause. The cross and the can only swept rows out of the ring; nothing touched the bags. The "sometimes" was Comfort/Leftovers.lua destroying what the loot filter refused with lootDestroy on.  Fix. No second destroy path. Leftovers gained Leftovers.Destroy(link, count, quest), which owes a count through the same Owe and pays it with the same Walk (split the owed count off a stack, pick up, check the cursor, DeleteCursorItem), under the same refusals, now one Kept(quality, quest) function Discard also uses. Apply's registration became Listen(on), and Settled() drops the bag events once nothing is owed with the switch off. UI/Feed.lua: opts.removable may be { tip, gone(entry) }, and Feed:Sweep calls gone for every entry it takes out, so the cross and the can both destroy. Feeds/Loot.lua: gone and a listed arrival call one local Destroy(who, link, count, quest), which refuses the group's drops; the list keys by ns.ItemKind, the id Leftovers pays against. trees.lua allows Feeds -> ns.Leftovers once.  Gate. 40-loot-watch.lua on a bag of its own: the cross on a row of two leaves three of five, a blue stays, the can on two linen rows leaves seven of ten and the wool alone, three listed linen landing leave four, then the strip and the control as before, and the bag event is unregistered at the end. 71-leftovers passes unchanged.
