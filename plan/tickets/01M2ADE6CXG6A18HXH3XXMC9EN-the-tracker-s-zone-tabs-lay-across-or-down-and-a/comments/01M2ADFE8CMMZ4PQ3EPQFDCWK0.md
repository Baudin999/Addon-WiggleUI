---
revision: 5
id: 01M2ADFE8CMMZ4PQ3EPQFDCWK0
---

Landed at b0da9c7 (amended). Shape. One strip object with an across flag, not a second widget: the two directions share the pooling, the keyed selection, the clipping and the rotation probe, and differ in one loop and one angle. Gate. ./scripts/check.sh at 0 warnings, 0 errors over 270 files, harness included. Watch for. The tabs are pooled, so the axis has to be set on the strip before Set() puts labels on it, and flipping it afterwards re-turns every label and moves every accent mark; Quests/Column.lua calls Across() first in Paint for that reason.
