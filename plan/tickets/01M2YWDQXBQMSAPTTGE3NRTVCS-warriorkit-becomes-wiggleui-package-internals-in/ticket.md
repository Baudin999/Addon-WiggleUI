---
revision: 5
id: 01M2YWDQXBQMSAPTTGE3NRTVCS
type: task
status: todo
title: "WarriorKit becomes WiggleUI: package, internals, installs"
---

The addon has not been warrior-only for months and the name said otherwise. It
is now WiggleUI, after the mouse wiggle that swaps the screen between two
themes (UI/Wiggle.lua).

Scope: the folder and both TOCs, every identifier and string in src/ and
scripts/, the slash command, the saved-variable table names, the art file
names, the README and docs, the live symlinks and saved variables in both
installs, and a gate that keeps the old name out.

Out of scope: plan/, docs/CHANGELOG.md and docs/POSTMORTEMS.md, which are
append-only records of what happened under the old name.
