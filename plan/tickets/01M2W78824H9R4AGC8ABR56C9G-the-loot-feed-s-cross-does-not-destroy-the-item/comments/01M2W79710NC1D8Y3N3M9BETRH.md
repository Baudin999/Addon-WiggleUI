---
revision: 5
id: 01M2W79710NC1D8Y3N3M9BETRH
---

Cause, unconfirmed. UI.Button set OnClick without registering, the one family of buttons left off UI.Press.Clicks. A plain Button's default registration is already LeftButtonUp, so this may not be the whole of it. Fix. 2d511d9: UI.Button registers through UI.Press.Clicks(button, "up", "LeftButton") when handed onClick. Gate. 40-loot-watch asserts wkEdge up and LeftButtonUp on both row buttons; check.sh 0/0. If it still fails live: see whether the row leaves the feed. Row gone and item kept means Leftovers.Destroy refused it (blue or better, quest item, or the split and pick up in Walk read back no cursor). Row still there means the click never arrived.
