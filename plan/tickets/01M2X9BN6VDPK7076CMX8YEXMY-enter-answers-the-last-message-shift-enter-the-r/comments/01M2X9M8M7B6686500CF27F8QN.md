---
revision: 5
id: 01M2X9M8M7B6686500CF27F8QN
---

Landed in 5ade8de. Rooms.lua keeps the heard order and the room last sent to; Feed.Handle feeds it off the answer and mine flags on KINDS. Window keeps 'answered', the heard count at the last room choice: Enter moves only when a line arrived after it, so Go, a rail pick, Tab and /g all consume it and Shift-Enter does not. Shift is read in ChatWindow.Answer when the line fills, because both keys run the client's own OPENCHAT and nothing of ours is in that stack. Shift-Enter goes through the new UI.Bound.Command and only lands where the player's own set leaves the key free. Not yet tried in the live client: check that Shift-Enter opens the line (IsShiftKeyDown must still read true at OnEditFocusGained) and that a pick on the rail after a whisper keeps Enter in the pick.
