---
revision: 5
id: 01M31F3PJZX6C91X4GJRFQV8AQ
---

Landed at 119d0fc9.

The gesture. Shift and the right button, which is what the card proposed. Said here rather than left implicit: the two plain buttons are spent on wearing and re-taking, and shift is already the modifier this page uses for a second reading on a hover, so it is the modifier a second reading of a press belongs on. The dropdown is the shape to reach for the moment a third thing wants a home on a toggle, and rename is the obvious third; it is more window than one gesture needs today.

Fix. SetRow.Forgotten puts UI.Ask in front of ns.Sets.Remove, naming the set and saying that `set undo` puts it back, which it does because Remove takes its own snapshot. The toggle's hover carries the line. The forget door is the fourteenth name Character would have had on ns.Sets, which scripts/trees.lua caps at thirteen, so the page's own reach came down by two to pay for it and for Sets.Line: Sets.Press took the talent-switch-or-clothes decision off the page, and the two Sets.Entry call sites went behind one door.

Gate. 52-set-page.lua drives both halves through the pointer with the button named: the question is asked and refused, then asked and answered, then undone. The last set is forgotten through the gesture at the foot rather than through the store, which is what proves the row goes back to thirty-six and the circles go with it.
