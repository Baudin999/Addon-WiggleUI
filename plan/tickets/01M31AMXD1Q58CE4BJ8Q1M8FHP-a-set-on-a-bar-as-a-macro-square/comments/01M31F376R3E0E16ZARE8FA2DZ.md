---
revision: 5
id: 01M31F376R3E0E16ZARE8FA2DZ
---

Landed at 119d0fc9. A macro square, as written.

Fix. AdHoc.Carry grows a "set" kind taking the name and the picture. Bars.Arm writes type=macro and macrotext=ns.Sets.Line(record.name) at every apply rather than saving the text, so a rename rewrites the square instead of breaking it; Sets.Rename calls ns.AdHoc.Renamed before the name moves. Sets.Line lives in Sets/Feature.lua, which is the file that owns the word. The drag is UI/Carry.lua off the toggle, because the client cursor has no kind for a set, and the panel square takes it through DropSquare's `carried` hook, which was already there for the talent window.

Not done, deliberately. The ring's own squares take drops off the client cursor only. A ring is up for the second a key is held, so there is nothing to drag onto; the page is where a bar is designed and that is where the hook went.

The per set key. Bind the bar and push toward the square, which is what the card said would make 01M1Y4JJCMRCGEMB0DT4MET6W7 unnecessary. That card is already dropped.

Gate. 76-adhoc.lua drops a set on the page's middle square through the pointer, reads the macrotext back, renames the set and reads it again, forgets the set and then types the square's own line at the slash prompt rather than comparing it to a copy written in the section. 52-set-page.lua drags the toggle and reads the line off its hover.
